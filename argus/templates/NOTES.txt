1. View the logs for Argus by running this command:
{{- if contains "ClusterIP"  .Values.service.type }}
  kubectl logs -f $(kubectl get pods --namespace=kube-monitoring -l app={{ .Values.service.name }} -o name) -c argus -n kube-monitoring
{{- end }}
